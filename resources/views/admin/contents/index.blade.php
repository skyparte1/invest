@extends('layouts.app') @section('title','Conteúdos | Administração') @section('content')
<main class="container py-5"><div class="d-flex justify-content-between align-items-center"><h1>Conteúdos</h1><a class="btn btn-primary" href="{{ route('admin.conteudos.create') }}">Novo conteúdo</a></div><x-admin-nav />
<div class="table-responsive"><table class="table align-middle"><thead><tr><th>Título</th><th>Categoria</th><th>Status</th><th>Ações</th></tr></thead><tbody>@foreach($contents as $content)<tr><td>{{ $content->title }}</td><td>{{ $content->category->name }}</td><td>{{ $content->is_published?'Publicado':'Rascunho' }}</td><td><a href="{{ route('admin.conteudos.edit',$content) }}">Editar</a> <form class="d-inline" method="POST" action="{{ route('admin.conteudos.destroy',$content) }}" onsubmit="return confirm('Excluir este conteúdo?')">@csrf @method('DELETE') <button class="btn btn-sm btn-link text-danger">Excluir</button></form></td></tr>@endforeach</tbody></table></div>{{ $contents->links() }}</main>@endsection
