@extends('layouts.app') @section('title','Investimentos | Administração') @section('content')
<main class="container py-5"><div class="d-flex justify-content-between align-items-center"><h1>Investimentos</h1><a class="btn btn-primary" href="{{ route('admin.investimentos.create') }}">Novo investimento</a></div><x-admin-nav />
<div class="table-responsive"><table class="table align-middle"><thead><tr><th>Nome</th><th>Categoria</th><th>Risco</th><th>Status</th><th>Ações</th></tr></thead><tbody>@foreach($investments as $investment)<tr><td>{{ $investment->name }}</td><td>{{ $investment->category->name }}</td><td>{{ $investment->riskLabel() }}</td><td>{{ $investment->is_published?'Publicado':'Rascunho' }}</td><td><a href="{{ route('admin.investimentos.edit',$investment) }}">Editar</a> <form class="d-inline" method="POST" action="{{ route('admin.investimentos.destroy',$investment) }}" onsubmit="return confirm('Excluir este investimento?')">@csrf @method('DELETE') <button class="btn btn-sm btn-link text-danger">Excluir</button></form></td></tr>@endforeach</tbody></table></div>{{ $investments->links() }}</main>@endsection
