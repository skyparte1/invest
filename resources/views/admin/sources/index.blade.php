@extends('layouts.app') @section('title','Fontes | Administração') @section('content')
<main class="container py-5"><div class="d-flex justify-content-between align-items-center"><h1>Fontes</h1><a class="btn btn-primary" href="{{ route('admin.fontes.create') }}">Nova fonte</a></div><x-admin-nav />
<div class="table-responsive"><table class="table align-middle"><thead><tr><th>Instituição</th><th>Título</th><th>Vínculos</th><th>Ações</th></tr></thead><tbody>@foreach($sources as $source)<tr><td>{{ $source->institution }}</td><td>{{ $source->title }}</td><td>{{ $source->contents_count + $source->investments_count }}</td><td><a href="{{ route('admin.fontes.edit',$source) }}">Editar</a> <form class="d-inline" method="POST" action="{{ route('admin.fontes.destroy',$source) }}" onsubmit="return confirm('Excluir esta fonte?')">@csrf @method('DELETE') <button class="btn btn-sm btn-link text-danger">Excluir</button></form></td></tr>@endforeach</tbody></table></div>{{ $sources->links() }}</main>@endsection
